{
  "name": "planu-center-monorepo",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix server && npm install --prefix workshop-app",
    "build": "npm run build --prefix workshop-app",
    "start": "npm start --prefix server",
    "seed": "npm run seed --prefix server"
  }
}
